export declare class TemplateManager {
    private backupDir;
    private templateDir;
    private workingDir;
    constructor(workingDir?: string);
    copyAllTemplates(force?: boolean): Promise<void>;
    private backupFile;
    private compareFiles;
    private confirmOverwrite;
    private createReadlineInterface;
    private handleTemplateFile;
}
