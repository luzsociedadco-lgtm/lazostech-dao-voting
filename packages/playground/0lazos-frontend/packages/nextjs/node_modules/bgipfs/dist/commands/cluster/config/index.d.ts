import { BaseCommand } from '../../../base-command.js';
export default class Init extends BaseCommand {
    static description: string;
    static flags: {
        force: import("@oclif/core/interfaces").BooleanFlag<boolean>;
        mode: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
    };
    static hooks: {
        postrun: string[];
    };
    run(): Promise<void>;
    private collectEnvValues;
    private copyFileIfNotEmpty;
    private copyWithConfirmation;
    private generateSecret;
    private getPeerAddresses;
    private initializeCluster;
    private readCurrentEnv;
    private validateInput;
    private validateSecretInput;
}
